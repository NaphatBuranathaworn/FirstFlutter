import 'package:flutter/material.dart';

class AppContainerAnimate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AppContainerAnimate')),
    );
  }
}
